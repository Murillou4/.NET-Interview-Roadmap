# Copy this file to `.env.local` if you need local overrides.
# The app works without extra environment variables.
VITE_APP_TITLE=".NET Interview Roadmap"

# Optional: Google OAuth Client ID for Drive appDataFolder sync.
# Create a Web OAuth client in Google Cloud and add your app origin to Authorized JavaScript origins.
VITE_GOOGLE_CLIENT_ID=""
